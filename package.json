{
  "name": "cyt-job-backup-database",
  "description": "Firebase job to backup database data run every day",
  "scripts": {
    "lint": "eslint ./src",
    "serve": "firebase emulators:start --inspect-functions --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/firestore": "^4.9.8",
    "firebase-admin": "^9.2.0",
    "firebase-functions": "^3.11.0"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-google": "^0.14.0",
    "firebase-functions-test": "^0.2.0"
  }
}
